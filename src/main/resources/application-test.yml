spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:cexpilot_test}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 8

logging:
  level:
    com.cexpilot: debug

# 测试环境经 nginx 代理访问交易所公共行情（本机直连不通时也能测试）
exchange:
  binance:
    base-url: http://43.160.213.63:18080
  okx:
    base-url: http://43.160.213.63:18081
